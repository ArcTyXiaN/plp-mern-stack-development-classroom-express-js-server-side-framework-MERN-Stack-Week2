{
  "name": "project",
  "version": "1.0.0",
  "description": "1. Run: npm init -y\r 2. Install dependencies:\r    npm install express body-parser uuid dotenv\r 3. Create a `.env` file using `.env.example`\r 4. Run the server:\r    node server.js",
  "main": "middleware.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "uuid": "^13.0.0"
  }
}
